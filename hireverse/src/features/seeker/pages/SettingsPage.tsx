import LogoutButton from "@core/components/ui/LogoutButton";

const SettingsPage = () => {
    return (
        <div>
            <LogoutButton/>
        </div>
    );
}

export default SettingsPage;
